apiVersion: v1
kind: Service
metadata:
  labels:
    application: skipper-ingress
    component: valkey
  name: skipper-ingress-valkey
  namespace: kube-system
spec:
  clusterIP: None
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    application: skipper-ingress
    component: valkey
  type: ClusterIP
