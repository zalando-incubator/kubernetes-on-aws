apiVersion: v1
kind: Service
metadata:
  name: cluster-autoscaler
  namespace: kube-system
  labels:
    application: cluster-autoscaler
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
    name: main-port
  selector:
    application: cluster-autoscaler
