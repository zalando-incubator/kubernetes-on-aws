apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-aws-iam-controller
  namespace: kube-system
  labels:
    application: kubernetes
    component: aws-iam-controller
    version: v0.2.0
spec:
  replicas: 1
  selector:
    matchLabels:
      deployment: kube-aws-iam-controller
  template:
    metadata:
      labels:
        application: kubernetes
        component: aws-iam-controller
        deployment: kube-aws-iam-controller
        version: v0.2.0
      annotations:
        logging/destination: "{{.Cluster.ConfigItems.log_destination_infra}}"
    spec:
      serviceAccountName: kube-aws-iam-controller
      priorityClassName: "{{ .Cluster.ConfigItems.system_priority_class }}"
      # running with hostNetwork to bypass metadata service block from pod
      # network.
      hostNetwork: true
      containers:
      - name: kube-aws-iam-controller
        image: container-registry-test.zalando.net/teapot/kube-aws-iam-controller-test:pr-62-6
        args:
        - --debug
        - "--assume-role={{.Cluster.LocalID}}-worker"
        ports:
          - containerPort: 8080   # the port for the health endpoint
        resources:
          limits:
            cpu: "{{.ConfigItems.kube_aws_iam_controller_cpu}}"
            memory: "{{.ConfigItems.kube_aws_iam_controller_mem_max}}"
          requests:
            cpu: "{{.ConfigItems.kube_aws_iam_controller_cpu}}"
            memory: "{{.ConfigItems.kube_aws_iam_controller_mem_max}}"
        livenessProbe:
          httpGet: 
            path: /healthz
            port: 8080
          failureThreshold: 5
          initialDelaySeconds: 10
          periodSeconds: 10
      tolerations:
      - key: node.kubernetes.io/role
        value: master
        effect: NoSchedule
      nodeSelector:
        node.kubernetes.io/role: master
