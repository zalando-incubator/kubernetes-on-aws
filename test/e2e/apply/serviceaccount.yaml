apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{{APPLICATION}}}-{{{COMPONENT}}}"
  labels:
    application: "{{{APPLICATION}}}"
    component: "{{{COMPONENT}}}"
  annotations:
    iam.amazonaws.com/role: "{{{IAM_ROLE_ARN}}}"
